export interface Repo {
    name: string;
    description: string;
    html_url: string;
    owner: string;
}

export interface Card {
    title: string;
    description: string;
    link: string;
    owner: string;
}